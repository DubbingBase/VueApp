// Include the main app module
include ':app'

// Include Cordova plugins only if they exist
File cordovaPluginsDir = new File('./capacitor-cordova-android-plugins/')
if (cordovaPluginsDir.exists()) {
    include ':capacitor-cordova-android-plugins'
    project(':capacitor-cordova-android-plugins').projectDir = cordovaPluginsDir
}

apply from: 'capacitor.settings.gradle'
